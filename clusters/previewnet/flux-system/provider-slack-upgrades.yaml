apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: slack-upgrades
  namespace: flux-system
spec:
  type: slack
  channel: hashio-alerting
  secretRef:
    name: slack
