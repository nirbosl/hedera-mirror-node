apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: github-dispatch
  namespace: flux-system
spec:
  eventMetadata:
    cluster: testnet-eu
    namespaces: common,testnet-citus
    summary: "Trigger testnet-eu deployment"
  eventSeverity: info
  eventSources:
    - kind: HelmRelease
      name: mirror
      namespace: testnet-citus
  inclusionList:
    - ".*.test.*succeeded.*"
  providerRef:
    name: github-dispatch

