# SPDX-License-Identifier: Apache-2.0

name: End-to-end Tests

on:
  pull_request:
    branches:
      - "main"
      - "release/**"
    paths:
      - ".github/workflows/acceptance.yaml"
      - "test/**"
  push:
    branches:
      - "main"
      - "release/**"
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch"
        required: true
        type: string

permissions:
  contents: read
  packages: read
  pull-requests: read

defaults:
  run:
    shell: bash

env:
  LC_ALL: C.UTF-8

jobs:
  acceptance:
    name: acceptance (${{ matrix.stream-type }})
    runs-on: hiero-mirror-node-linux-large
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix:
        stream-type:
          - RECORD
    #          - BLOCK # re-enable when bumping to CN & BN releases with 16-slot sub merkle tree support
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@b09bb98e06d4d774595224525879c09bc6e98c40 # v2.20.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || '' }}

      - name: Login to Docker Hub
        uses: step-security/docker-login-action@bd6978fd4ef9a5f78130095b298b8a721afcb0d8 # v4.5.1
        if: vars.DOCKERHUB_USERNAME != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Run end-to-end tests
        uses: ./.github/actions/run-e2e
        with:
          build-changed-modules-only: "true"
          image-source: jvm
          stream-type: ${{ matrix.stream-type }}

      - name: Run k6 tests
        if: matrix.stream-type != 'BLOCK'
        env:
          BASE_URL: "http://127.0.0.1:38081"
          DEFAULT_DURATION: "3s"
          DEFAULT_GRACEFUL_STOP: "1s"
          DEFAULT_VUS: 2
          RUN_OPCODE_TESTS: "true"
        uses: ./.github/actions/k6
